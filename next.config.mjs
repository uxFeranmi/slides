/** @type {import('next').NextConfig} */
const nextConfig = {
	// swcMinify: false,
	// distDir: 'docs',
	pageExtensions: ['page.tsx', 'm.ts'],
	output: 'export',
	// outDir: 'build',
	// basePath: '/slides',
};

export default nextConfig;
